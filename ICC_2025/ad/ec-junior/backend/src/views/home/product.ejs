<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= product.Name %> - EC Junior</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header', { user }) %>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12">
                <h1><%= product.name %></h1>
                <p class="lead"><%= product.description %></p>
                <h2 class="text-primary">$<%= product.price.toFixed(2) %></h2>
                <p><strong>Stock Available:</strong> <%= product.stock %></p>
                <p><strong>Category:</strong> <%= product.category %></p>

                <% if (user && product.stock > 0) { %>
                    <form method="post" action="/cart/add">
                        <input type="hidden" name="productId" value="<%= product.id %>">
                        <div class="mb-3">
                            <label for="quantity" class="form-label">Quantity</label>
                            <input type="number" class="form-control" id="quantity" name="quantity" value="1" min="1" max="<%= product.stock %>" style="width: 150px; font-size: 1.1rem; padding: 0.5rem;">
                        </div>
                        <button type="submit" class="btn btn-primary">Add to Cart</button>
                    </form>
                <% } else if (!user) { %>
                    <a href="/account/login" class="btn btn-primary">Login to Purchase</a>
                <% } else { %>
                    <button class="btn btn-secondary" disabled>Out of Stock</button>
                <% } %>

                <a href="/" class="btn btn-outline-secondary mt-3">Back to Products</a>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>