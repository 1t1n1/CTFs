<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Confirmation - EC Junior</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header', { user }) %>

    <div class="container mt-4">
        <div class="alert alert-success">
            <h4 class="alert-heading">Order Confirmed!</h4>
            <p>Thank you for your order. Your order has been successfully placed.</p>
        </div>

        <% if (flag2) { %>
            <div class="alert alert-warning">
                <h5>Congratulations! 🎉</h5>
                <p><strong>Flag:</strong> <%= flag2 %></p>
            </div>
        <% } %>

        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5>Order Details - #<%= order.id %></h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Order Date:</strong> <%= new Date(order.orderDate).toLocaleDateString() %></p>
                        <p><strong>Status:</strong> <%= order.status %></p>
                        <p><strong>Shipping Address:</strong> <%= order.shippingAddress %></p>
                        <p><strong>Total Amount:</strong> $<%= order.totalAmount.toFixed(2) %></p>
                    </div>
                </div>

                <div class="card mt-3">
                    <div class="card-header">
                        <h5>Items Ordered</h5>
                    </div>
                    <div class="card-body">
                        <% orderItems.forEach(item => { %>
                            <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                                <div>
                                    <h6 class="mb-0"><%= item.name %></h6>
                                    <% if (item.description) { %>
                                        <small class="text-muted"><%= item.description %></small>
                                    <% } %>
                                </div>
                                <div class="text-end">
                                    <div>$<%= item.price.toFixed(2) %> × <%= item.quantity %></div>
                                    <strong>$<%= (item.price * item.quantity).toFixed(2) %></strong>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">What's Next?</h5>
                        <p class="card-text">Your order is being processed and will be shipped soon.</p>
                        <a href="/" class="btn btn-primary w-100">Continue Shopping</a>
                        <a href="/account/profile" class="btn btn-outline-secondary w-100 mt-2">View Profile</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>