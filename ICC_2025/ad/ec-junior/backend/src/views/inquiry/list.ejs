<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Inquiries - EC Junior</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/header', { user }) %>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>My Inquiries</h2>
            <a href="/inquiry/new" class="btn btn-primary">New Inquiry</a>
        </div>

        <% if (inquiries.length === 0) { %>
            <div class="alert alert-info">
                You haven't submitted any inquiries yet.
            </div>
        <% } else { %>
            <div class="list-group">
                <% inquiries.forEach(inquiry => { %>
                    <a href="/inquiry/<%= inquiry.id %>" class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1"><%= inquiry.subject %></h5>
                            <small><%= new Date(inquiry.createdAt).toLocaleDateString() %></small>
                        </div>
                        <p class="mb-1"><%= inquiry.message.substring(0, 100) %><%= inquiry.message.length > 100 ? '...' : '' %></p>
                        <small>ID: <%= inquiry.id %></small>
                    </a>
                <% }); %>
            </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
